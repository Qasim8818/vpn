# Continue.dev Example Usage

1. Open VS Code and install the Continue extension.
2. Place the provided `config.json` in this folder.
3. In the Continue chat, type `@codebase` to index your local files.
4. Now you can ask questions about your codebase, and Continue will use local embeddings via Ollama.

**All data and embeddings are processed locally.**
